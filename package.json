{
  "name": "anthropic-watch",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that polls Anthropic status.anthropic.com and notifies Wesley via alerthub-ingress (Gemini-summarised) on degradation — runs on a non-Anthropic LLM substrate so the notify path survives Claude outages.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.90.0"
  }
}
